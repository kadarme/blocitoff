<% unless item.name.nil? %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.days_left %></td>
    <td><%= link_to "", [item.list, item], method: :delete, class: 'glyphicon glyphicon-ok' %></td>
  </tr>
<% end %>

