<% if current_user %>
  <h4>Add an item:</h4>
  <%= form_for [list, Item.new] do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name, placeholder: 'Enter item' %>
      <%= f.submit %>
    <% end %>
<% end %>

